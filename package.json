{
  "devDependencies": {
    "coffee-script": "^1.10.0"
  },
  "scripts": {
    "build": "cat svg.coffee app.coffee | ./node_modules/.bin/coffee --bare --compile --stdio > build/build.js",
    "test": "npm run build; node build/build.js > out.svg; open out.svg"
  }
}
